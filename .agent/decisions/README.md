# decisions

Architecture decision records (ADRs). Each file captures a durable design choice and its rationale.

| Record | Decision |
|--------|----------|
| [`0001-engine.md`](0001-engine.md) | Simulation is deterministic; the renderer never mutates world state |

Add new records as `NNNN-short-title.md` with increasing numbers.
