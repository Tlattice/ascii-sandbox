# .github

GitHub automation: CI, issue templates, agent scripts, and workflow definitions.

Development is issue-driven: open an issue, label it `ready_to_plan`, then `ready_to_code` to trigger the agent pipeline.
